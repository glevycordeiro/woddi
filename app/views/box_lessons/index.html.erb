<div class="container boxes-container">
  <h1><%= box_lesson.box.name %></h1>
    <div class="row">
      <% @box_lessons.each do |box_lesson| %>
        <%= render "card_box_lesson", box_lesson: box_lesson %>
      <% end %>
    </div>
</div>
